import { MenuBar } from "../components/MenuBar"

export const Usuários = () => {

    console.log('window', window.location.pathname);
    return (
        <div>
            <MenuBar/>
        </div>
    )
}
